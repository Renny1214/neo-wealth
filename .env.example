# Optional override when API is hosted on a different origin.
# Default: /api (same-origin via Vite dev proxy or Express in production)
# VITE_API_BASE=/api

# Server port (production start script)
# PORT=3001

# Comma-separated allowed CORS origins for the API server
# ALLOWED_ORIGINS=http://localhost:5173
